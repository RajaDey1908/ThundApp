import {
    StyleSheet,
} from 'react-native';

module.exports = StyleSheet.create({
    Container: {
        flex: 1
    },
    MainContainer: {
        flex: 1,
        alignItems: 'center',
    },
    ImageContainer: {
        width: 40,
        height: 40,
        marginTop: 90
    },
    TextStyle: {
        color: '#0250a3',
        textAlign: 'center',
        fontSize: 30,
        marginTop: 10,
    }
});
